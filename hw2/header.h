#ifndef HEADER_H
#define HEADER_H

#include <iostream>
#include <string>
#include <cstring>
#include <sstream>
#include <map>
#include <queue>
#include <fstream>
#include <bitset>

using namespace std;

#endif /* HEADER_H */

#ifdef _DEBUG
    #define DEBUG(x) do { std::cerr<<x<<endl; } while (0)
#else
    #define DEBUG(x)
#endif